# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Binaries
stripe

# Local Claude Code config
.claude/

# TypeScript build artifacts
tsconfig.tsbuildinfo

# Raw recipe corpus (Brewer's Friend Kaggle dump) — facts-only derived outputs
# are committed, but the raw member-submitted recipes are never shipped/committed.
# See ~/.claude/plans/i-found-this-crazy-noble-pumpkin.md (legal/data posture).
src/modules/corpus-lab/raw/
src/modules/corpus-lab/offline/out/cloud.ndjson
