// HS mirror of /browse/compare — keeps the user inside HS chrome
// when comparing recipes from /browse. Inner CompareRecipesPage uses
// classic styling for now.
export { default, metadata } from "@/../app/betabuilder/browse/compare/page";
