// /browse is the community browse page. HS chrome is provided by ClientShell.
// Inner BrowseRecipesPage still uses classic Tailwind styling — to be brought
// under HS overrides in a follow-up.
export { default, metadata } from "@/../app/betabuilder/browse/page";
